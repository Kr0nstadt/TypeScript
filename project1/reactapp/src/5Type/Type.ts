// 5. Объявите переменные с явной типизацией:
//  age — целое число.
//  name — строка.
//  isStudent — булево значение.
//  hobbies — массив строк (например, ['reading', 'sports']).
//  favoriteScores — кортеж (tuple) из двух чисел: [number, number].
//  contact — объект с полями phone: string и email: string.
let age : number;
let name : string;
let isStudent : boolean;
let hobbies : string[] =['reading', 'sports'];
let favoriteScores : [number, number];
let contact :{
    phone : string,
    email : string
} 